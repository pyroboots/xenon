[-] types
    [x] array
    [x] dicts
    [x] custom type
    [x] func
    [x] enum
    [x] type methods
    [x] optional fields
    [x] distinct instances

[-] builtins
    [x] cast
    [x] try
    [x] import
    [x] forEach

[-] std libs
    [x] array (transform, filter, slice, enumerate, measure, append, prepend, insert, contains, find, reduce, reverse, first, last, remove, sort)
    [x] dict (filter, enumerate, measure, add, remove, contains, find, keys, values, merge, getOr, update)
    [x] string (split, trim, contains, replace, replaceAll, join, measure, substring, startsWith, endsWith, charAt, upper, lower, isEmpty, isBlank, repeat, format)
    [x] bool (and, or, not, nand, nor, xor, xnor, ternary, whichever, any, all, asNum, fromNum)
    [x] num (odd, even, positive, negative, integer, clamp, round, parse, between)
    [x] func (getArgMap, getReturnType)
    [x] math (abs, acos, asin, atan, ceil, cos, deg, exp, floor, log, max, min, pi, pow, rad, rand, sin, sqrt, tan, round, sign, clamp)
    [x] fs (read, write, exists, list, join)
    [x] json (parse, stringify)
    [x] time (now, sleep, format)
    [x] path (join, baseName, dirName, extension)
    [x] enumlib (toInt, fromInt, members)
    [x] console (out, outLn, err, errLn, in, inLn)

[-] future
    [x] type extends
    [x] REPL
    [x] better error line numbers
    [x] regex/match (str.match, str.test)